export interface iValid {
    valid: {
        connect: boolean,
        accesspoint: boolean,
        wsensors: boolean,
        clock: boolean,
        display1: boolean,
        display2: boolean,

        account: boolean
    }
}